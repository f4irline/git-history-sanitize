apiVersion: apps/v1
kind: Deployment
metadata:
  name: support-case-api
spec:
  template:
    spec:
      containers:
        - name: api
          image: registry.example.invalid/support-case-api:stable
