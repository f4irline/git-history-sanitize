# Paths whose working-tree content is masked in the sandbox and whose Git
# history must therefore be unavailable to agents. A trailing slash denotes a
# directory. Paths are relative to the repository root.
secret.json
infra/
