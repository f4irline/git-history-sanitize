# Copy this to .env and fill in values. Never commit .env.

# Host path to mount as /workspace in the devcontainer (default: the example
# directory). This local override keeps the moved prototype self-contained.
WORKSPACE_PATH=/Users/tlepola/Documents/dev/projects/work/dvv/ai/sandbox-testing/proto
